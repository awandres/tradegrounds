import 'package:flutter/material.dart';

class DeliveryCalendarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Center(child: Text('Delivery Calendar'),),
      ),
    );
  }
}