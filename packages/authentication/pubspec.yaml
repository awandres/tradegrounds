name: authentication
description: Provides authentication sign up and sign in services for storeground vendor users.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.4.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  provider: ^4.0.4
  cloud_firestore: ^0.13.5
  firebase_auth: ^0.16.1
  intl: ^0.15.8
  http: ^0.12.0+2
  url_launcher: ^5.7.0
  shared_preferences: ^0.5.3+1
  # cloud_firestore: ^0.13.5
  # firebase_auth: ^0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - assets/images/storegroundsLogo.png
